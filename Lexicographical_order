def lexicographical_order(rows):
    char='A'
    for i in range(1, rows + 1):
        for j in range(1, i + 1):
            print(char, end=" ")
            char = chr(ord(char)+1)
        print()
num_rows = int(input("Enter number of rows:"))
lexicographical_order(num_rows)
